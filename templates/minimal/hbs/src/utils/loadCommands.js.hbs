/**
 * @file loadCommands.js
 * @description This module dynamically loads command modules from the 'src/commands' directory
 * and registers them with the Commander.js program.
 * @author Ion Gireada
 */

/**
 * Dynamically loads command modules from the 'src/commands' directory
 * and registers them with the Commander.js program.
 *
 * @param {object} program - The Commander.js program object.
 */
function loadCommands(program) {}

module.exports = loadCommands
